[build]
builder = "NIXPACKS"
buildCommand = "cd apps/web-enterprise && pnpm install && pnpm build"

[deploy]
startCommand = "cd apps/web-enterprise && PORT=4320 node .next/standalone/apps/web-enterprise/server.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

